import React from "react";
import MainTemplate from "../../components/MainTemplate";

const Catalogue = () => {
  return <MainTemplate sectionName="Catalogo"></MainTemplate>;
};

export default Catalogue;
